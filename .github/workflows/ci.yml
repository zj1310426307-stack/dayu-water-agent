name: CI

on:
  push:
    branches: [main, "phase/**"]
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: dayu_agent
          POSTGRES_USER: dayu_agent
          POSTGRES_PASSWORD: dayu_agent
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U dayu_agent -d dayu_agent"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 20
    env:
      DAYU_TEST_DATABASE_URL: postgresql+psycopg://dayu_agent:dayu_agent@127.0.0.1:5432/dayu_agent
      PYTHONDONTWRITEBYTECODE: "1"
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: python -m pip install -e ".[dev]"
      - name: Verify migrations
        env:
          DATABASE_URL: postgresql+psycopg://dayu_agent:dayu_agent@127.0.0.1:5432/dayu_agent
        run: |
          python -m alembic upgrade head
          python -m alembic downgrade base
          python -m alembic upgrade head
      - name: Verify hermetic settings tests
        env:
          DATABASE_URL: postgresql+psycopg://ambient:ambient@invalid.example/dayu
          MODEL_PROVIDER: openai
          SESSION_STORE: postgres
          OPENAI_API_KEY: ambient-test-placeholder
        run: python -m pytest tests/unit/test_config.py
      - name: Quality gates
        run: |
          python -m ruff check .
          python -m mypy src/dayu_agent
          python scripts/generate_openapi.py --check
          python -m pytest --cov=dayu_agent --cov-report=term-missing --cov-fail-under=80
          python -m pip check
          python -m compileall -q src tests
